namespace TouchTracking
{
    /*
     * Classe enum per scelta della gesture su schermo
     */
    public enum TouchActionType
    {
        Entered,
        Pressed,
        Moved,
        Released,
        Exited,
        Cancelled
    }
}
